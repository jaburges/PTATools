/Azure Plugin.zip
/Deprecated
Azure Plugin/PTATools.wiki/
wiki-temp/
.cursor/
*.env
.DS_Store
**/.DS_Store

# Build artifacts (produced by deploy-staging.yml and manual Kudu pushes)
/build/
/azure-plugin.zip
/azure-plugin-flat.zip
/pta-tools.zip

# Unrelated vendor drop sitting next to the repo (don't ship)
/Junotoys_v2.30.0/

# Site salvage, summer park assets, and the iOS app (own private repo)
/salvage/
/summer-placeholder/
/ios-app/

# Local summer park backups (DB dumps, secrets)
.local-backups/

# Container image build scratch — assembled from the migration export at build
# time and can contain a full wp-content tree
infra/wp-image/.context/
infra/wp-image/.ctest/

# Local scratch: diagnostic probes, held-back patches, and pre-deploy config
# snapshots kept for rollback
.tmp/


# Local zip/build artifacts from infra scripts
.build/
