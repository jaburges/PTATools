# Plugins that must be in every wilderptsa-wp image.
# slug<tab>version<tab>main-file
# Fetched from wordpress.org if the migration export does not contain them.
# Do not rely on Dockerfile RUN into /var/www/html — the WordPress base
# image declares VOLUME /var/www/html, so those RUN files are discarded.
woocommerce-gateway-stripe	10.9.0	woocommerce-gateway-stripe.php
redis-cache	2.8.0	redis-cache.php
multiple-roles	1.3.8	multiple-roles.php
